<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Details of <%= book.title %></title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<div class="container">
  <h1>Details of "<%= book.title %>"</h1>
  <ul>
    <li><b>Autor:</b> <%= book.author %></li>
    <li><b>Publication date:</b> <%= book.publicationdate ? new Date(book.publicationdate).toDateString() : "No avalible" %> </li>
    <li><b>In Store:</b> <%= book.inStore ? 'yes' : 'No' %></li>
  </ul>
  <a href="/" class="back-button">⬅Home</a>
  <button onclick="confirmDelete('<%= book._id %>')" class="delete-button">Delete</button>
</div>

<script>
  function confirmDelete(id) {
    if (confirm("¿Are you sure you want to delete this book?")) {
      window.location.href = `/delete/${id}`;
    }
  }
</script>

</body>
</html>
